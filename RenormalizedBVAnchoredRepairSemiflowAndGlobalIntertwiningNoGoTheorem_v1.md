# Renormalized BV Anchored Repair-Semiflow and Global Intertwining No-Go Theorem v1

**Claim:** CBF.T39

**Date:** 2026-08-30

**Status:** exact no-go for literal identity-projection equality of the full
T34 tree and T35 quantum radial repair flows in the allowed local
renormalizable counterterm orbit; unique all-orders local-formal q79 BV
normalization preserving the selected value, tadpole and Hessian; formal
pointed QJ1 closed constructively and action-jet QJ2 closed conditionally;
upper physical selection, metric-normalized QJ2, gravitational QJ0 and the
fixed-coupling interacting endpoint remain open.

## 1. Result

CBF.T38 identified the following strong sufficient condition for quantum
tadpole protection:

```text
pi Phi_t=Psi_t pi.                                    (1.1)
```

Here `Phi_t` is an upper repair flow, `Psi_t` is the repair flow generated by
the lower effective action, and `pi` is the physical projection. If the upper
attractor projects to `H`, equation (1.1) forces `H` to remain a lower fixed
point and hence gives QJ1.

The first question is whether (1.1) should be imposed on the entire radial
flow after quantum correction. In the actual T35 one-loop action, the answer
is no under the currently declared radial identification.

The fermion determinant contains

```text
-kappa_F q4_* h^4 log(h^2/mu^2).                      (1.2)
```

Every allowed local counterterm is in

```text
C_even=span{1,h^2,h^4}.                              (1.3)
```

The fifth derivative of (1.2) is

```text
-48 kappa_F q4_*/h,                                  (1.4)
```

whereas the fifth derivative of every element of (1.3) is zero. Since
`q4_*>0`, `H>0` and `kappa_F>0`, the full quantum potential cannot differ
from the tree potential by a constant on any open positive radial interval.
With the identity radial projection and a common radial metric, the complete
tree and quantum negative-gradient flows therefore cannot be equal.

This is not a failure of QJ1. It proves that global nonlinear flow equality is
too strong: it would erase the genuine logarithmic quantum interaction.

The correct replacement is pointed repair naturality. Let

```text
j_H^2 f=(f(H),f'(H),f''(H)).                          (1.5)
```

There is a unique local counterterm `C_H[f]=a0+a2 h^2+a4 h^4` with the same
two-jet as `f`. Its coefficients are

```text
a0=b0-(5/8)H b1+(1/8)H^2 b2,
a2=(3b1-Hb2)/(4H),
a4=(Hb2-b1)/(8H^3),                                  (1.6)
```

where `(b0,b1,b2)=j_H^2 f`. Define

```text
R_H=I-C_H.                                            (1.7)
```

For the full local-formal q79 BV scale generator

```text
beta_s^BV
 =beta_s^action
 +beta_s^measure
 +beta_s^determinant
 +beta_s^cycle,                                       (1.8)
```

the anchored evolution is

```text
partial_s Gamma_s^H=R_H beta_s^BV(Gamma_s^H).         (1.9)
```

It satisfies

```text
partial_s j_H^2 Gamma_s^H=0.                         (1.10)
```

The existing q79 Costello recursion, Lorentzian
Stueckelberg-Petermann comparison and zero local anomaly class make (1.9) a
well-defined QME-compatible formal normalization at every finite
perturbative bidegree. Its normalized comparison maps obey the RG scale
cocycle. No observed value, fitted coefficient or new free counterterm enters.

Equation (1.10) gives

```text
Gamma_s^H'(H)=0,                                     (1.11)
```

and preserves the action Hessian at `H`. With a common selected tangent
metric, the tree and quantum repair vector fields therefore agree at `H` and
have the same linearization there. They need not, and in T35 do not, agree at
higher nonlinear order.

This closes QJ1 constructively at the local formal anchored-scheme tier. It
does not prove that upper MTT selects that finite normalization as the
physical q79 law. Physical QJ1 and the endpoint acceptance counters therefore
remain open.

## 2. The full differentiated BV pushforward

Let the regulated field space split into retained and eliminated BV fields,

```text
E=E_IR direct_sum E_UV,
```

and let `L_s(h)` be the ultraviolet Lagrangian cycle. Include its BV
half-density explicitly:

```text
Psi_s(h,phi_IR)
 =integral_(L_s(h))
   exp[-S_s(h,phi_IR,chi)/hbar] rho_s(h,chi)^(1/2).
                                                               (2.1)
```

The local formal effective action is

```text
Gamma_s=-hbar log Psi_s.                              (2.2)
```

Differentiating in the radial direction gives the T37 identity

```text
partial_h Gamma_s
 =<partial_h S_s-hbar partial_h log rho_s^(1/2)>_s
  +B_s/Psi_s.                                         (2.3)
```

The density term contains both ordinary measure transport and determinant
data. `B_s` is the moving-cycle or BV-BFV boundary contribution. Thus a
quantum radial Ward identity has four typed sources:

1. action insertion;
2. measure transport;
3. determinant density;
4. cycle or boundary transport.

Only their sum has to vanish. Requiring every term separately to vanish would
be stronger than QJ1 and is not used.

The RG-scale derivative is a different derivative from (2.3). In a BV exact
RG presentation it produces a local formal vector field on effective
actions. We write that vector field as (1.8), retaining the same four-source
typing. The q79 graph recursion gives its coefficientwise local realization;
the anomalous Master Ward identity and zero anomaly class keep it tangent to a
QME-compatible Stueckelberg-Petermann orbit.

Three parameters must remain distinct:

```text
s_RG   = regulator or renormalization scale coordinate,
s_rep  = auxiliary negative-gradient repair coordinate,
t_Lor  = physical Lorentzian time.                    (2.4)
```

T39 identifies none of them with another.

## 3. What the q79 corpus already supplies

The source audit closes more of the analytic scaffolding than the short
blocker label suggests.

### 3.1 Local formal interaction and QME

On every declared on-shell q79 chart:

- Epstein-Glaser time-ordered products exist coefficientwise;
- the complete local gauge-anomaly vector is exactly zero;
- local QME breakings are removable by finite local counterterms;
- an all-orders formal QME scheme exists;
- the quantum BRST differential is nilpotent; and
- admissible gauge-fixing changes induce BV-cohomology isomorphisms.

This makes the local formal interacting algebra and its normalization orbit
available. It does not select one radial normalization condition.

### 3.2 Graphwise effective actions

The first-order Costello presentation supplies one local counterterm at every
finite graph bidegree and an exact RG composition law. For a fixed singular
splitting the divergent counterterm recursion is unique. Its finite local
part remains related to other prescriptions by the Stueckelberg-Petermann
group.

The Lorentzian bridge supplies a normalized local comparison with

```text
Z(0)=0,
Z'(0)=id,                                             (3.1)
```

and restores the QME and Ward identities coefficientwise.

### 3.3 Cycle and determinant data

At free finite-shell tier, the q79 Hodge splitting supplies an exact
Lagrangian ultraviolet cycle and a QME-preserving BV pushforward. Along the
certified diffeomorphism presentation orbit:

```text
relative APS spectral flow =0,
relative BV-BFV flux        =0,
determinant transport       =canonical.              (3.2)
```

These equalities cannot simply be copied to radial background motion. A
change of the gauge-invariant Higgs radius is not a diffeomorphism or frame
presentation change.

The common determinant phase is a `U(1)` torsor. It cancels from normalized
expectation values and every positive source derivative. The determinant
magnitude and relative phases do not cancel and remain in (2.3).

### 3.4 What is still absent

The corpus does not supply:

- a fixed-coupling nonperturbative interacting BV pushforward;
- uniform interacting regulator removal;
- a physical boundary theorem beyond the certified transport orbit;
- a selected global interacting state; or
- an upper-action theorem choosing the radial anchored normalization.

The construction below is consequently local and formal, not a
nonperturbative physical vacuum theorem.

## 4. Global radial intertwining no-go

Let the tree and quantum radial potentials be

```text
P_*(h),
Gamma(h)=P_*(h)+V_F(h)+C(h),                          (4.1)
```

where `C` lies in (1.3). Assume:

1. the radial projection is the identity on one open interval `I` contained
   in `(0,infinity)`;
2. the two repair laws use the same positive radial metric `g(h)` on `I`;
3. `kappa_F>0`; and
4. only the declared power-counting local counterterm orbit (1.3) is used.

The repair vector fields are

```text
X_tree=-g^-1 dP_*,
X_quant=-g^-1 dGamma.                                (4.2)
```

If the flows intertwine through the identity for every initial point in `I`,
their generators agree:

```text
X_tree=X_quant.                                       (4.3)
```

Positivity of `g` makes (4.3) equivalent to

```text
d(V_F+C)=0.                                           (4.4)
```

Thus `V_F+C` is constant on `I`, and its fifth derivative must vanish.

The polynomial part of `V_F+C` has zero fifth derivative. Direct
differentiation gives

```text
d^5/dh^5 [h^4 log(h^2/mu^2)]=48/h.                  (4.5)
```

Equations (1.2) and (4.5) therefore yield (1.4), which is nonzero throughout
`I`. This contradicts (4.4).

Hence:

```text
full identity-projection tree/quantum repair-flow equality
is impossible in the declared T35 counterterm orbit.                 (4.6)
```

This no-go is deliberately bounded. A selected nonlinear field
redefinition, a nonidentity physical projection, or a different quantum
radial metric could evade its assumptions. None is currently emitted by the
q79 source, and introducing one merely to cancel (1.4) would require a new
source theorem.

The theorem does not say that quantum corrections destroy the fixed point.
It says that preserving the entire classical nonlinear trajectory would
incorrectly remove their nonpolynomial content.

## 5. Unique pointed anchor retraction

For `H>0`, restrict the two-jet map (1.5) to (1.3). In the basis
`(1,h^2,h^4)`, its matrix is

```text
[1  H^2   H^4 ]
[0  2H    4H^3]
[0  2    12H^2].                                     (5.1)
```

Its determinant is

```text
16H^3>0.                                              (5.2)
```

The inverse of (5.1) gives (1.6). Consequently

```text
j_H^2 C_H=j_H^2,
j_H^2 R_H=0,
C_H^2=C_H,
R_H^2=R_H.                                            (5.3)
```

The construction is linear. Applied to (1.8),

```text
R_H beta_s^BV
 =R_H beta_s^action
 +R_H beta_s^measure
 +R_H beta_s^determinant
 +R_H beta_s^cycle.                                  (5.4)
```

Therefore no arbitrary allocation of the cancellation among the four terms
is needed. The total local two-jet determines exactly one radial counterterm.

For QJ1 alone, one scalar equation would leave a two-dimensional
counterterm family. QJ1 plus the action-Hessian part of QJ2 fixes `a2,a4`
uniquely, leaving only `a0`. Adding the value condition fixes `a0`; in a
normalized nongravitational QFT that last constant is quotient data, while in
gravity it remains a physical vacuum-normalization question.

## 6. Anchored local-formal BV evolution

Let `beta_s^BV` be the coefficientwise q79 BV exact-RG generator in one
declared local chart. Define (1.9), with initial condition

```text
j_H^2 Gamma_0^H=j_H^2 P_*.                           (6.1)
```

Applying `j_H^2` and using (5.3) proves (1.10). Hence

```text
Gamma_s^H(H)   =P_*(H),
Gamma_s^H'(H)  =0,
Gamma_s^H''(H) =P_*''(H)                             (6.2)
```

at every finite formal perturbative order.

Existence follows coefficientwise. At one graph bidegree, all predecessor
counterterms have already been constructed. The raw local radial coefficient
has a finite two-jet at `H`; equations (1.6) emit its unique allowed local
anchor counterterm. The q79 zero anomaly class removes the only nontrivial
ghost-one obstruction to continuing the QME recursion. Induction gives the
formal inverse limit.

Composition is not an extra assumption. Between any two scales, use the
unique normalized Stueckelberg-Petermann comparison satisfying (6.2).
Composing comparisons across an intermediate scale again gives a normalized
local comparison satisfying (6.2). Uniqueness in the radial counterterm
sector makes it equal to the direct comparison. Thus

```text
U_H(s3,s2) U_H(s2,s1)=U_H(s3,s1).                    (6.3)
```

For a nonautonomous scale generator this is an evolution family. On the
extended space `(s,Gamma)`, it is an ordinary formal semiflow. It is not the
physical repair-time flow.

The construction uses three finite normalization conditions and leaves zero
free radial coefficients. That is not the same as deriving the conditions
from the upper physical action.

## 7. Pointed repair-flow intertwining

At each RG scale, let the effective radial repair vector field be

```text
X_s^H(h)=-g(h)^-1 partial_h Gamma_s^H(h),             (7.1)
```

and let

```text
X_*(h)=-g(h)^-1 partial_h P_*(h).                    (7.2)
```

Equation (6.2) gives

```text
X_s^H(H)=X_*(H)=0.                                   (7.3)
```

Differentiating (7.1)-(7.2), all derivatives of `g^-1` multiply the already
zero first-derivative difference. The Hessian equality in (6.2) therefore
gives

```text
(X_s^H)'(H)=X_*'(H)                                  (7.4)
```

whenever both descriptions use the same tangent metric at `H`.

Equations (7.3)-(7.4) are the correct pointed first-jet intertwining:

```text
j_H^1 X_s^H=j_H^1 X_*.                               (7.5)
```

They preserve the fixed point and its linear stability. Higher derivatives
are allowed to differ and encode real quantum interactions.

For the T35 matched remainder, in `x=h/H`,

```text
rho(x)=x^4(3/2-log(x^2))-2x^2+1/2.                  (7.6)
```

Its derivatives at `x=1` are

```text
(rho,rho',rho'',rho''',rho'''',rho''''')
 =(0,0,0,-16,-64,-48).                               (7.7)
```

Thus T35 already realizes (7.5) exactly while retaining nonzero nonlinear
quantum vertices. The global no-go and the pointed construction agree.

## 8. QME and Ward compatibility

The radial counterterms are gauge-invariant local functionals of the Higgs
radius. They have:

```text
ghost number       =0,
antifield degree   =0,
s(h)               =0,
Delta_BV C_H       =0,
(C_H,C_H)          =0.                               (8.1)
```

At classical level they are tangent to the master-equation locus. At quantum
level, the exact q79 local anomaly vector is zero, so the registered
BRST-primitive induction supplies a QME-compatible representative at every
formal order.

This proves compatibility, not selection. The three directions
`1,h^2,h^4` are ghost-zero gauge-invariant finite normalization directions.
They are not gauge-anomaly representatives. Neither the QME nor an ordinary
gauge Ward identity distinguishes the coefficients (1.6).

For the declared boundaryless Lorentzian local bridge, the auxiliary BV-BFV
factor cancels. On transported diffeomorphism orbits, the cycle flux is zero.
For an arbitrary physical boundary, a universal anchored normalization still
requires a boundary-compatible source theorem. T39 does not infer one from
the local result.

## 9. QJ classification after T39

The three quantum-jet clauses now separate as follows.

### QJ1

```text
local-formal anchored scheme: closed constructively;
upper-selected physical q79 law: open.                (9.1)
```

The construction shows that no preferred full interacting state is required
to impose the radial anchor. It does not prove that the physical source does
so.

### QJ2

```text
effective-action Hessian at H: preserved formally;
physical normalized Hessian: open.                    (9.2)
```

The missing datum is the selected tangent metric or wave-function
normalization relating the upper and lower radial coordinates. No numerical
mass or pole claim follows from an unnormalized action Hessian.

### QJ0

For one connected normalized QFT sector, a common determinant phase and
field-independent action constant cancel from normalized observables and
positive source derivatives. This part is retyped as quotient data rather
than a physical exit.

For a gravitational absolute action, the vacuum constant contributes to the
stress tensor and cannot be quotiented away. Gravitational QJ0 remains open.

## 10. Exact scientific boundary

Closed here:

- the four-term action/measure/determinant/cycle BV generator typing;
- exact exclusion of global identity radial repair-flow equality for the T35
  logarithmic loop in the allowed local counterterm orbit;
- proof that this no-go does not obstruct pointed QJ1;
- the explicit inverse (1.6) for an arbitrary raw BV two-jet;
- linear cancellation of the total four-term jet without arbitrary
  term-by-term allocation;
- unique all-orders local-formal anchored q79 BV evolution;
- preservation of value, tadpole and action Hessian at `H`;
- pointed fixed-point and tangent-flow intertwining;
- exact T35 execution with fifth normalized jet `-48`;
- QME compatibility of the anchored formal scheme; and
- separation of normalized QFT QJ0 from gravitational QJ0.

Still open:

- upper-MTT selection of the anchored normalization;
- the fixed-coupling interacting q79 BV pushforward;
- uniform interacting regulator removal;
- a selected physical tangent metric or wave-function normalization;
- arbitrary physical-boundary cycle cancellation;
- gravitational vacuum normalization;
- numerical RG, threshold, pole and uncertainty transport; and
- held-out observable comparison.

No observed value, fit, new continuous parameter or new discrete selector is
used. The three finite anchor conditions determine three local coefficients,
leaving no free radial normalization coefficient. They remain a scheme until
selected by the upper action.

The physical counters remain

```text
accepted packets: 0/3,
accepted rows:    0/7.                               (10.1)
```

The genuine frontier change is conceptual and mathematical: the full global
intertwining target is now excluded in the actual one-loop radial theory, and
the exact physical target is reduced to selection of the already-constructed
pointed anchored normalization plus its tangent metric.

## 11. Primary external context

Costello's BV renormalization theorem supplies graphwise local counterterms
and compatible effective actions:

- Kevin Costello, *Renormalisation and the Batalin-Vilkovisky formalism*,
  <https://arxiv.org/abs/0706.1533>.

The renormalized quantum BV algebra and anomalous Master Ward identity are
the appropriate formal language for the QME statements:

- Klaus Fredenhagen and Katarzyna Rejzner,
  *Batalin-Vilkovisky formalism in perturbative algebraic quantum field
  theory*, <https://arxiv.org/abs/1110.5232>.

BV-BFV pushforward explains why a moving cycle or boundary term must be
tracked rather than silently discarded:

- Alberto Cattaneo, Pavel Mnev and Nicolai Reshetikhin,
  *Perturbative quantum gauge theories on manifolds with boundary*,
  <https://arxiv.org/abs/1507.01221>.

BV exact RG supplies canonical scale maps and a Polchinski-form generator:

- Roberto Zucchini, *Exact renormalization group in Batalin-Vilkovisky
  theory*, <https://arxiv.org/abs/1711.01213>.

The normalized comparison orbit is governed by the perturbative
renormalization group:

- Romeo Brunetti, Michael Duetsch and Klaus Fredenhagen,
  *Perturbative Algebraic Quantum Field Theory and the Renormalization
  Groups*, <https://arxiv.org/abs/0901.2038>.

These sources justify the general BV/RG machinery. They do not select
`H_T34`, the radial anchor conditions, a q79 physical metric or the missing
upper-action theorem.

## 12. Reproduction

Run:

```powershell
python build_renormalized_bv_anchored_repair_semiflow.py
python verify_renormalized_bv_anchored_repair_semiflow.py
python -m unittest tests.test_renormalized_bv_anchored_repair_semiflow -v
python verify.py
```

The generated packet is
`renormalized_bv_anchored_repair_semiflow.packet.json`.
