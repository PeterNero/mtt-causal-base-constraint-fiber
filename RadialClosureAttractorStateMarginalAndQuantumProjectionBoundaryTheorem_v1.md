# Radial Closure-Attractor, State-Marginal, and Quantum-Projection Boundary Theorem v1

**Claim:** CBF.T38

**Date:** 2026-08-30

**Status:** exact T34 radial square-completion, nonlinear-attractor,
invariant-state and formal local q79 state-extension theorem; exact
repair-semiflow projection criterion for QJ1; the selected physical
renormalized BV intertwiner, preferred global state and physical QJ1 remain
open.

## 1. Result

CBF.T37 reduced quantum fixed-point protection to one sharp equality,

```text
omega_q79(h)=H_T34,                                    (1.1)
```

for a selected zero-source state, or to an equivalent quantum-BV radial Ward
primitive. The current q79 corpus supplies nonempty local formal positive
interacting state spaces but no preferred state. This theorem asks whether
that full preferred-state problem is really necessary for the radial row.

It is not necessary at the finite/formal radial tier.

The fixed-source T34 tree potential is

```text
P_*(h)=q4_* h^4-4c q2_* h^2,
c=(15/log(448)) Lambda^2,
H^2=(2q2_*/q4_*)c.                                    (1.2)
```

Exact arithmetic in `Q(sqrt(13))` gives

```text
q4_* (2q2_*/q4_*)=2q2_*.
```

Consequently

```text
P_*(h)-P_*(H)=q4_* (h^2-H^2)^2.                       (1.3)
```

No new coefficient or fitted number appears. On the selected radial domain
`h>=0`, (1.3) has the unique zero-defect minimum `h=H`.

The negative-gradient repair law is explicitly solvable. On the open broken
basin `h>0`, every trajectory converges to `H`, and `delta_H` is the unique
invariant probability measure. On the closed half-line, `h=0` is also an
unstable fixed point, but it has strictly higher tree action; `delta_H` remains
the unique zero-defect or ground-state measure.

This selects a radial state marginal without selecting the entire q79 state.
For any normalized positive local formal q79 state `omega_H` on the fiber at
background `H`, evaluation at `H` defines

```text
Omega_H=omega_H composed with (ev_H tensor id).        (1.4)
```

It is normalized, positive and BRST-compatible whenever the already-declared
fiber differential commutes with evaluation. Every such extension obeys
(1.1), although its gauge and matter correlations can differ.

The physical promotion condition is now exact. Let `Phi_s` be the selected
upper repair semiflow, `Psi_s` the renormalized lower effective semiflow and
`pi` the physical projection. If

```text
pi composed with Phi_s=Psi_s composed with pi           (1.5)
```

and the lower radial flow is generated by `-g^-1 dGamma`, then the projected
attractor is fixed and

```text
dGamma(H)=0.                                           (1.6)
```

Thus (1.5), unlike generic action pushforward, is a sufficient naturality
theorem for QJ1.

The current physical source does not yet emit (1.5). Indeed the actual T35
bare fermion determinant gives

```text
V_F'(H)/(kappa_F Lambda^3)
=-100.1144836274302795882555068876969...,              (1.7)
```

so its truncated lower flow does not fix `H`. T38 therefore closes the
finite/formal radial-attractor and state-marginal construction, but does not
promote physical QJ1 or change the `0/3`, `0/7` endpoint counters.

## 2. Exact square completion

The source-pinned finite coefficients are

```text
q2_*=(14+sqrt(13))/3,
q4_*=(356+25sqrt(13))/27,
R_* =(3106+4sqrt(13))/4393.                            (2.1)
```

Multiplication in `Q(sqrt(13))` proves

```text
q4_* R_*=2q2_*.                                       (2.2)
```

Because `H^2=R_*c`, equation (2.2) gives

```text
4c q2_*=2q4_* H^2.                                    (2.3)
```

Substitution in (1.2) yields

```text
P_*(h)
=q4_* h^4-2q4_*H^2h^2
=q4_*(h^2-H^2)^2-q4_*H^4.                             (2.4)
```

This proves (1.3). Since `q4_*>0` and `H>0`, the relative potential is
nonnegative and vanishes on `h>=0` only at `H`.

The constant `-q4_*H^4` was already the value `P_*(H)`. Square completion has
not changed the action or introduced a vacuum normalization. It has only made
the selected zero set visible.

The result uses the T33 source freeze. The upstream coordinate `t_*` is not
varied again in (2.4). Reopening that coordinate would return to the different
joint action whose bare minimum is the already-proved `t=0` branch.

## 3. Nonlinear repair flow

Use the tree potential itself as the radial repair functional. Its
negative-gradient flow is

```text
d h/ds_rep=-partial_h P_*(h)
          =4q4_* h(H^2-h^2).                          (3.1)
```

The symbol `s_rep` is a repair parameter. It is not identified with Lorentzian
time or with the compact shared-circle phase.

Set `u=h^2`. Then

```text
d u/ds_rep=8q4_*u(H^2-u).                             (3.2)
```

For `u0>0`, separation of variables gives

```text
u(s)=H^2/
 [1+(H^2/u0-1)exp(-8q4_*H^2 s)].                     (3.3)
```

The denominator stays positive for every `s>=0`, so the flow is forward
complete on `(0,infinity)`. Equation (3.3) immediately proves

```text
lim_(s->infinity) h(s)=H                              (3.4)
```

for every `h0>0`.

The two fixed points on `[0,infinity)` are `0` and `H`. Linearization of (3.1)
gives

```text
beta'(0)=+4q4_*H^2>0,
beta'(H)=-8q4_*H^2<0.                                (3.5)
```

Thus zero is unstable and `H` is strictly stable. The Lyapunov identity is

```text
dP_*/ds_rep=-(partial_h P_*)^2<=0.                    (3.6)
```

The executable checks (3.3), its semigroup composition law and contraction
toward `H` from five points spanning both sides of the minimum.

## 4. Unique radial invariant state

Let `phi_s` denote the flow (3.3) on `(0,infinity)`. If `nu` is an invariant
Borel probability measure, then for every bounded continuous `f`,

```text
int f(h) dnu(h)=int f(phi_s(h)) dnu(h).                (4.1)
```

By (3.4), `f(phi_s(h))->f(H)` pointwise. Dominated convergence in (4.1)
therefore gives

```text
int f(h)dnu(h)=f(H).                                   (4.2)
```

Hence

```text
nu=delta_H.                                            (4.3)
```

The same argument proves weak convergence

```text
(phi_s)_* nu0 -> delta_H                              (4.4)
```

for every initial probability on the positive basin.

There is a second, purely state-algebraic proof that does not refer to an
initial distribution. Let `omega` be a positive radial state with the required
finite moment and suppose it has zero selected closure cost:

```text
omega((h^2-H^2)^2)=0.                                 (4.5)
```

In the GNS representation,

```text
||pi_omega(h^2-H^2)Omega_omega||^2=0.                 (4.6)
```

The spectral measure of `h` is therefore supported on the zero set of
`h^2-H^2`. Positivity of the radial coordinate removes `-H`, leaving

```text
supp(nu_omega)={H},
omega(h)=H,
omega((h-H)^2)=0.                                     (4.7)
```

On the three-point exact witness `h/H in {1/2,1,2}`, the dimensionless costs
are

```text
{9/16,0,9}.
```

A probability has zero cost only when its weights are `{0,1,0}`.

On the closed radial half-line, mixtures of `delta_0` and `delta_H` are
flow-invariant because zero is an exact fixed point. They are not zero-defect
ground states: equation (1.3) assigns strictly positive cost to zero. The two
selection statements must not be conflated:

```text
unique invariant state on the selected positive basin: delta_H,
unique zero-defect state on the closed radial half-line: delta_H.          (4.8)
```

## 5. Extension over the q79 state cone

The q79 local formal state theorem supplies, on every declared admissible
chart, a nonempty set `S_form(H)` of normalized formally positive physical
states on the BRST observable algebra `A_phys,H`. It does not choose one
element of that set.

Introduce the radial background algebra and its fiber algebra as

```text
A_base=C_0((0,infinity)) tensor A_phys,H.              (5.1)
```

For the formal perturbative statement, (5.1) means the corresponding
algebraic tensor product and formal positive cone. Evaluation is a unital
star-homomorphism,

```text
ev_H tensor id:A_base -> A_phys,H.                     (5.2)
```

For any `omega_H in S_form(H)`, define (1.4). Then

```text
Omega_H(1)=1,
Omega_H(a^*a)=omega_H(a(H)^*a(H))>=0.                 (5.3)
```

If the BRST differential is fiberwise and commutes with evaluation, then

```text
Omega_H(Qa)=omega_H(Q_H a(H))=0.                      (5.4)
```

For every scalar background observable `f(h)`,

```text
Omega_H(f(h))=f(H).                                    (5.5)
```

In particular, `Omega_H(h)=H`.

This construction proves that nonuniqueness of the gauge/matter state does
not imply nonuniqueness of the radial marginal. It does not select a preferred
Hadamard seed, one global cosmological state, a fixed-coupling Hilbert
completion or a nonperturbative vacuum.

The `delta_H` statement concerns the homogeneous or central radial background
coordinate. It does not assert that local Higgs fluctuations vanish. Those
fluctuations remain operators in the fiber algebra at background `H` and are
governed by the still-open interacting state and renormalized action.

## 6. Repair-semiflow projection naturality

The correct projection theorem is stronger than ordinary functoriality of an
action integral.

Let `X` and `Y` be upper and lower state spaces, let `Phi_s` and `Psi_s` be
continuous semiflows, and let `pi:X->Y` be a continuous surjection satisfying
(1.5). Suppose every upper trajectory converges to one point `x_*`.

For `y in Y`, choose `x` with `pi(x)=y`. Then

```text
Psi_s(y)=Psi_s(pi(x))
        =pi(Phi_s(x))
        ->pi(x_*).                                    (6.1)
```

Thus `y_*=pi(x_*)` is the unique global lower attractor. At the measure level,

```text
pi_* (Phi_s)_* nu=(Psi_s)_* pi_*nu,                  (6.2)
```

and therefore

```text
pi_*delta_x*=delta_y*.                                (6.3)
```

Now suppose the lower radial semiflow is the negative gradient flow of the
renormalized effective action,

```text
d h/ds=-g(h)^-1 dGamma(h),
g(H)>0.                                                (6.4)
```

If the upper attractor projects to `H`, equation (6.1) makes `H` a lower fixed
point. Equation (6.4) then proves

```text
dGamma(H)=0.                                           (6.5)
```

This is QJ1.

Differentiating (1.5) at the attractor also gives

```text
Dpi_x* A_up=A_low Dpi_x*.                             (6.6)
```

Equation (6.6) is the operator part of a QJ2 bridge, but numerical Hessian
matching still requires the selected tangent metric or wave-function
normalization already identified by CBF.T36.

The distinction from CBF.T36 is exact. A fiber integral can be perfectly
natural as an action pushforward while generating a tadpole. It then fails
(1.5). Repair-semiflow naturality is an additional physical compatibility
property, not a consequence of ordinary integration.

## 7. T35 obstruction to immediate promotion

At the T34 point the upper tree repair flow vanishes. The T35 bare determinant
has the radial derivative (1.7) in MSbar at `mu=Lambda`. Its two declared
determinant-line candidates give

```text
complex determinant: -10.1437179808726814804... Lambda^3,
Pfaffian half:       -5.0718589904363407402... Lambda^3.                (7.1)
```

Both are nonzero. Before metric inversion, the fixed-point intertwining defect
is the tadpole covector itself:

```text
Dpi beta_up(H)-beta_low(H)=V_F'(H).                   (7.2)
```

Therefore the actual truncated bare projection does not satisfy (1.5).

CBF.T37 proves that a QJ1-compatible counterterm line exists, and CBF.T35
proves the full QJ1-QJ2-QJ0 subtraction is unique once those certificates are
selected. Neither result proves that the physical renormalized BV flow is the
intertwining flow required here.

The remaining QJ1 exit is consequently one of:

1. a same-source renormalized BV construction proving (1.5);
2. an explicit radial quantum-BV Ward primitive with Stokes and no cycle
   boundary; or
3. a stronger full-loop cancellation theorem whose resulting effective flow
   satisfies (1.5).

Simply declaring `delta_H` to be the physical state would skip this exit. T38
does not do so.

## 8. Exact scientific boundary

Closed here:

- the exact T34 square completion (1.3);
- the closed-form nonlinear radial repair flow;
- global attraction on the positive broken basin;
- uniqueness of the invariant radial probability on that basin;
- uniqueness of the zero-defect radial state on the closed half-line;
- the GNS support theorem forcing `omega(h)=H`;
- positive normalized extension over every existing local formal q79 state;
- separation of the radial marginal from full-state uniqueness;
- the repair-semiflow projection theorem sufficient for QJ1; and
- the exact T35 failure of that intertwining at bare one-loop tier.

Still open:

- selection of the repair semiflow as the physical zero-source state law;
- a physical q79 BV pushforward satisfying (1.5);
- a preferred global interacting or cosmological q79 state;
- QJ1 at the accepted physical tier;
- QJ2 tangent normalization and Hessian intertwining;
- QJ0 determinant-line or gravitational vacuum normalization;
- the full bosonic, ghost, gauge and gravitational loop completion; and
- RG, pole, uncertainty and held-out observable transport.

No observed value, fitted coefficient, new continuous parameter or hidden
state choice is used. The endpoint counters remain

```text
physical packets: 0/3,
physical rows:    0/7.                                (8.1)
```

The genuine advance is that the preferred-state blocker has been factored.
The radial marginal is now exact and unique at the selected finite/formal
repair tier. What remains is not arbitrary vacuum choice; it is one typed
quantum projection-intertwining theorem.

## 9. Primary external context

The use of local state spaces rather than one generally preferred vacuum is
consistent with locally covariant QFT and its state-space functor:

- Brunetti, Fredenhagen and Verch, *The Generally Covariant Locality
  Principle*, arXiv:math-ph/0112041;
- Hollands and Ruan, *The State Space of Perturbative Quantum Field Theory in
  Curved Spacetimes*, arXiv:gr-qc/0108032.

The refusal to infer one universal preferred state from covariance alone is
also consistent with Fewster and Verch, *Dynamical Locality and Covariance*,
arXiv:1504.00586. That no-go does not prevent a special same-source q79 state;
it requires such a state to be separately constructed.

BV pushforward and effective actions provide the appropriate language for the
remaining quantum intertwiner, but do not automatically preserve a chosen
radial attractor; see Cattaneo, Mnev and Reshetikhin, *Perturbative Quantum
Gauge Theories on Manifolds with Boundary*, arXiv:1507.01221.

The square completion, flow solution, invariant-measure proof and
state-pullback construction in T38 are elementary consequences of the locked
MTT source. The cited literature supplies context, not the selected value
`H_T34` or the missing physical q79 intertwiner.

## 10. Reproduction

Run:

```powershell
python build_radial_closure_attractor_state_marginal.py
python verify_radial_closure_attractor_state_marginal.py
python -m unittest tests.test_radial_closure_attractor_state_marginal -v
python verify.py
```

The generated packet is
`radial_closure_attractor_state_marginal.packet.json`.
