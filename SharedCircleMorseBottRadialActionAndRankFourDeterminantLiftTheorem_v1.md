# Shared-Circle Morse-Bott Radial Action and Rank-Four Determinant Lift Theorem v1

**Claim:** CBF.T42

**Date:** 2026-08-30

**Status:** exact finite/local-formal construction of a single q79-rooted
Morse-Bott action, shared-circle vacuum, radial restriction, pointed
repair/tangent intertwiner and rank-four determinant-equivalent BV
pushforward to the complete T39 anchored remainder. This is a constructive
right inverse and a finite `G0` model. It is not yet a selected physical q79
upper action, operator equivalence or interacting four-dimensional BV source.

## 1. Result

CBF.T41 isolated three independent physical gluing gates. The first was

```text
G0: one upper field-only action and BV contraction whose pushforward emits
    the anchored lower action, with the radial fixed point and tangent repair
    square preserved under one source root.
```

The existing pieces did not even show that such a coupled object was
algebraically possible on the q79 finite carrier. The q79 Closure Dynamics
result now supplies more structure than was available when T41 was written:

```text
W_fin = R^3_D direct_sum R^3_E,
P     = rank-two Reynolds projector,
Q     = I-P, rank(Q)=4,
J     = J_DE, J^2=-I,
[J,P]=[J,Q]=0.
```

The plane `Ran(P)` is therefore one complex line. Its norm gives a
phase-independent radial coordinate

```text
r(w)=||Pw||,
```

and its `J` orbit gives the shared circle. This theorem couples that carrier
to the already-selected T34 radial data without choosing a phase in the
plane.

Write

```text
q=q4_*>0,
H=H_T34>0,
rho(x)=x^4(3/2-log(x^2))-2x^2+1/2,
a(x)=exp(rho(x)/2),
kappa=8qH^2.
```

Define one field-only action on the open set `r>0` by

```text
S_up(w)
 =q(r(w)^2-H^2)^2
 +(kappa/2)a(r(w)/H)||Qw||^2.                         (1.1)
```

Then:

1. `S_up` is invariant under the diagonal `S3` sheet action and the shared
   circle generated by `J`.
2. Its zero-defect minima are exactly

   ```text
   M_H={w in Ran(P): ||w||=H},
   ```

   one shared-circle orbit.
3. On the invariant zero section `Qw=0`, (1.1) is exactly the T38 relative
   action and its negative gradient projects to the T38 logistic repair law.
4. At every `w_* in M_H`, the Hessian is

   ```text
   Hess S_up(w_*)=kappa(I-T_*),                       (1.2)
   ```

   where `T_*` projects onto the circle tangent `Jw_*`. Thus the circle is the
   only zero mode and all five normal directions have one common stiffness.
5. The coefficient of `||Qw||^2` is uniquely fixed at the vacuum by that
   common-stiffness requirement. In the class

   ```text
   q(r^2-H^2)^2+alpha qH^2 a(r/H)||Qw||^2,
   ```

   the radial Hessian is `8qH^2`, the q79 normal Hessian is
   `2 alpha qH^2`, and equality forces `alpha=4`.
6. Integrating the four `Q` directions on the finite Euclidean BV
   Lagrangian gives exactly

   ```text
   -epsilon log[Z_Q(h)/Z_Q(H)]
    =2epsilon log a(h/H)
    =epsilon rho(h/H).                                (1.3)
   ```

   Hence the normalized effective action is the complete T39 anchored action
   shape, not just an action with the same first two derivatives.

Equation (1.3) is possible with no multiplicity choice because `rank(Q)=4` is
already selected by the finite q79 Reynolds carrier.

The construction is nevertheless not a physical source theorem. The
stiffness profile `a=exp(rho/2)` is reconstructed from the known lower
remainder. T42 proves existence, uniqueness inside the isotropic rank-four
class and exact compatibility. The next source theorem must derive the same
normal determinant from the selected physical q79 action rather than use
`rho` as input.

## 2. The finite q79 carrier

Let `A_3=(1/3)11^T`. In the ordered `D/E` basis,

```text
P=diag(A_3,A_3),
Q=I_6-P,
J=[[0,-I_3],[I_3,0]].                                 (2.1)
```

Exact arithmetic gives

```text
P^2=P, Q^2=Q, PQ=0, P+Q=I,
rank(P)=2, rank(Q)=4,
J^T=-J, J^2=-I, [J,P]=[J,Q]=0.                       (2.2)
```

Every `3 x 3` permutation matrix `Pi_g` acts diagonally as
`rho(g)=diag(Pi_g,Pi_g)`. It commutes with `P`, `Q` and `J`. Consequently the
quantities `||Pw||`, `||Qw||` and the action (1.1) are `S3 x U(1)` invariant.

At the finite-symbol tier, `(Ran(P),J)` is a Hermitian complex line. This is
not by itself a physical identification with the A35 Higgs line. Equality of
real dimension and a compatible circle action are insufficient; a parallel
same-source line map is still required for physical promotion.

## 3. Shared-circle vacuum and Hessian

Because `a(x)>0` for every `x>0`, variation in the `Q` directions forces
`Qw=0` at a critical minimum. The remaining relative action is

```text
S_rad(r)=q(r^2-H^2)^2.                                (3.1)
```

Its positive minimum is `r=H`. Thus the minimum set is the circle `M_H`.
Choose `w_*=He` with `||e||=1`, `Pe=e`, and set `t=Je`. Then

```text
E_*=ee^T,
T_*=tt^T,
P=E_*+T_*.
```

The radial Hessian is `8qH^2 E_*`. Since `a(1)=1`, the q79 normal block is
`kappa Q`. There are no mixed terms at `Qw=0`, so

```text
Hess S_up(w_*)
 =kappa(E_*+Q)
 =kappa(I-T_*).                                       (3.2)
```

This proves the Morse-Bott statement and identifies the shared-circle
direction as the sole zero mode. It also explains why the normal coefficient
is not arbitrary: requiring the radial and strain directions to be one
closure-normal bundle fixes `alpha=4` uniquely.

## 4. Exact radial projection

On `r>0`, let `p(w)=r(w)`. Its derivative is

```text
Dp_w(v)=<Pw,Pv>/r(w).                                 (4.1)
```

The zero section `Qw=0` is invariant under the gradient flow of (1.1). On it,

```text
Dp_w[-grad S_up]
 =4q r(H^2-r^2),                                      (4.2)
```

which is exactly the T38 repair law. At `w_*`,

```text
p(w_*)=H,
Dp X_up(w_*)=X_rad(H)=0,
Dp Hess(S_up)(w_*)=kappa Dp,
Hess(S_rad)(H)=kappa.                                 (4.3)
```

These are the fixed-point and tangent-generator squares demanded by the
finite version of `G0`. The theorem does not claim global projection
naturality away from the zero section: the `r` dependence of `a` creates a
normal-energy contribution to the off-section radial equation.

## 5. Rank-four determinant lift

T39 gives the exact dimensionless one-loop remainder

```text
rho(x)=x^4(3/2-log(x^2))-2x^2+1/2                    (5.1)
```

with

```text
(rho,rho',rho'',rho''',rho'''',rho''''') at x=1
 =(0,0,0,-16,-64,-48).                               (5.2)
```

For an isotropic positive rank-`m` normal Hessian

```text
K_m(h)=kappa a(h/H) I_m,
```

the normalized finite Gaussian contribution is

```text
(epsilon/2)log det[K_m(h)K_m(H)^-1]
 =(m epsilon/2)log a(h/H).                            (5.3)
```

Requiring (5.3) to equal `epsilon rho` and `a(1)=1` has the unique solution

```text
a_m(x)=exp(2rho(x)/m).                                (5.4)
```

The q79 normal rank is `m=4`, hence (5.4) becomes `a=exp(rho/2)`. Its contact
expansion in `y=x-1` begins

```text
a(1+y)=1-(4/3)y^3-(4/3)y^4-(1/5)y^5+O(y^6).          (5.5)
```

Thus `a(1)=1`, `a'(1)=a''(1)=0`, while the higher contact is nontrivial.
No nonconstant polynomial of degree at most two can obey these three
conditions. Cubic contact is the minimal possible nontrivial determinant
deformation, and the complete analytic profile is fixed uniquely by (5.1).

## 6. Finite BV interpretation

Pass to the shifted cotangent of the four-dimensional real normal space
`Ran(Q)`. The action (1.1) is independent of normal antifields, so its odd
self-bracket and finite BV Laplacian both vanish in the flat local model. It
therefore satisfies the finite QME. On the Euclidean Gaussian Lagrangian
`n^*=0`, normalized at `h=H`,

```text
Z_Q(h)/Z_Q(H)=a(h/H)^(-4/2)=exp[-rho(h/H)].           (6.1)
```

The induced action shift is exactly (1.3). The positive Gaussian also fixes
the finite determinant orientation in this model.

This is a valid finite/local-formal BV pushforward. It is not the selected
Lorentzian q79 gauge/matter BV pushforward. In particular, T42 has not shown
that the four Reynolds-normal directions have the statistics,
representations, kinetic symbols or correlations of the physical modes that
produced T35.

## 7. What is now closed

The constructed source has one content hash and one actual action, rather
than four unrelated certificates. Inside that model:

```text
finite constructed source root:             closed,
field-only upper action:                     closed,
S3 and shared-circle automorphisms:          closed,
shared-circle zero mode:                     closed,
T38 radial action restriction:               closed,
T38 repair projection on the zero section:   closed,
pointed tangent-generator square:            closed,
rank-four normalized BV/Gaussian pushforward:closed,
full T39 rho emission:                       closed,
new fitted or observed inputs:               zero.
```

This changes the frontier: a `G0`-shaped finite object is no longer merely
postulated, and the complete T39 remainder has an exact q79-rank-four
determinant normal form.

## 8. What remains physical

The physical exit is now one explicit operator/source equation. A selected
q79 physical normal block `K_phys(h)` must satisfy

```text
(1/2)log det[K_phys(h)K_phys(H)^-1]
 =rho(h/H)                                             (8.1)
```

on the same source root, together with a parallel operator intertwiner to the
finite `Q` block, the physical density and BV cycle, Lorentzian domains,
reality/statistics, charged representations and QME-preserving interacting
pushforward. Equation (8.1) is necessary for scalar radial equivalence. It is
not sufficient for operator equivalence or equality of non-radial
correlators.

Moreover, the map identifying the T34/A35 radial line with the orbit norm in
`Ran(P)` must be emitted by the universal shared-line geometry with its
connection. T42 constructs that map as a finite candidate; it does not prove
the physical parallel pullback.

Accordingly:

```text
finite determinant-equivalent G0 model:  1/1,
physical G0 gate:                         0/1,
physical G1 gate:                         0/1,
physical G2 gate:                         0/1,
physical packets accepted:               0/3,
physical rows accepted:                  0/7,
B.ACTION.01:                              open,
B.QFT.02:                                 open.
```

The distinction is substantive. T42 proves compatibility and supplies a
minimal exact target for the physical operator calculation. It does not use a
right inverse as evidence that nature selected its input.

## 9. Reproduction

```text
python build_shared_circle_morse_bott_rank_four.py
python verify_shared_circle_morse_bott_rank_four.py
python -m unittest tests.test_shared_circle_morse_bott_rank_four -v
python verify.py
```

The source hashes, exact matrices, series coefficients, determinant identity,
gate ledger and physical guard are recorded in
`shared_circle_morse_bott_rank_four.packet.json`.
